#version 450

// In-place scale: buf[i] *= s

layout(local_size_x = 256) in;

layout(set = 0, binding = 0) buffer Data { float data[]; };

layout(push_constant) uniform Params {
    int n;
    float s;
};

void main() {
    uint idx = gl_GlobalInvocationID.x;
    if (idx >= n) return;
    data[idx] *= s;
}
